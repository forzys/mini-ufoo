# book


### 初始化项目
```
git init
# 初始化

git remote add origin https://xx
# 关联远程仓库

git push --set-upstream origin master
# 关联远程( 上游 )分支

git add .
# 存储变更

git commit -m ''
# 提交本地

git push
# 推送到远程对应分支
```
